{
  "name": "json-server-vercel",
  "version": "1.0.0",
  "description": "Deploy JSON Server to Vercel",
  "main": "api/server.js",
  "scripts": {
    "start": "node api/server.js --port=3001"
  },
  "dependencies": {
    "json-server": "^0.17.0",
    "jsonwebtoken": "^9.0.0"
  }
}
