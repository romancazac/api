{
  "name": "json-server-vercel",
  "version": "1.0.0",
  "description": "Deploy JSON Server to Vercel",
  "main": "api/server.js",
  "scripts": {
    "start": "node api/server.js --port=3000"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "json-server": "^0.17.0",
    "jsonwebtoken": "^9.0.0"
  }
}
